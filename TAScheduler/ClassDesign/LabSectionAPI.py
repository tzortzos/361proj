from typing import Optional, Union

from TAScheduler.models import User, CourseSection
from TAScheduler.models import LabSection

class LabSectionAPI:

    @staticmethod
    def create_lab_section(
            lab_section_code: str,
            course_section_id: CourseSection,
            lab_days: str = '',
            lab_time: str = '',
            ta_id: User = None,
    ) -> int:
        pass

    @staticmethod
    def get_lab_section_by_lab_id(lab: int) -> Optional[LabSection]:
        """
        Using the autogenerated primary key returns a LabSection object
        """
        pass

    @staticmethod
    def get_all_lab_sections_from_course_section_and_course(
            course_section_code: str,
            course_code: str)\
            -> list[LabSection]:
        """
        Returns all LabSection objects associated with combination of specific course and course section
        """
        pass

    @staticmethod
    def edit_lab_section(
            lab: Union[LabSection, int],
    ) -> str:
        """
        Using the primary key identifier of the lab or the LabSection object itself, updates
        """
        pass